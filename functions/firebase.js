const firebase = require('firebase');

// Initializes and exports Firebase

module.exports = firebase.initializeApp({
  * replace * me *
  console.firebase.google.com Develop > Authentication > Web setup
});